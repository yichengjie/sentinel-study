server:
  port: 8080
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    sentinel:
      transport:
        dashboard: localhost:8858
        port: 8720
      filter:
        enabled: false
      eager: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
